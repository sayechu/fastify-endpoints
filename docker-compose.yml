services:
    db:
        container_name: pg_container
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        environment:
            POSTGRES_USER: root
            POSTGRES_PASSWORD: password
            POSTGRES_DB: mydb
        ports:
            - "5432:5432"
